spring:
##redis配置
  profiles: dev
  redis:
    database: 0
    host: 10.0.0.24
    port: 6379
    password:
    pool:
      max-active: 8
      max-wait: -1
      max-idle: 8
      min-idle: 0
    timeout: 0
  datasource:
    url: jdbc:mysql://10.0.0.20:3306/hardware?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
server:
  port: 8080